Vagrant.require_version '>= {{min_vagrant_version}}'

Vagrant.configure('2') do |config|
  config.ssh.username = '{{init_username}}'
  config.ssh.password = '{{init_password}}'
  config.vm.communicator = '{{communicator}}'
end
